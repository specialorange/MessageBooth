<% content_for :title, @product ? @product.name : "Display Photos" %>
<h1>Photo Index</h1>
<h1>All photos</h1>
<ul>
  <% @photos.each do |photo| %>
    <li>
    <%= link_to image_tag(photo.image.url(:thumb)), photo %>
    </li>
  <% end %>
</ul>
